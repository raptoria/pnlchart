import React, { useState, useCallback } from 'react';
import './header.scss';

interface HeaderProps {}

const Header: React.FC<HeaderProps> = ({}) => {
  return <header>Hum</header>;
};

export default Header;
